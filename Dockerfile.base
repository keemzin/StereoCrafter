# StereoCrafter WEBUI - BASE IMAGE
# Build once, use many times
# This includes all slow-to-build dependencies

FROM runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404

# Set working directory
WORKDIR /workspace

# Install system dependencies
RUN apt-get update && \
    apt-get install -y python3-tk git git-lfs && \
    git lfs install && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Clone from your fork (keemzin/StereoCrafter)
RUN git clone https://github.com/keemzin/StereoCrafter.git StereoCrafter && \
    cd StereoCrafter && \
    # Add upstream for syncing with original enoky repo
    git remote add upstream https://github.com/enoky/StereoCrafter.git

# Set working directory to StereoCrafter
WORKDIR /workspace/StereoCrafter

# Install Python dependencies (this is the slow part)
COPY requirements-docker.txt .
RUN pip install --no-cache-dir -r requirements-docker.txt && \
    pip install --no-cache-dir huggingface_hub[cli]

# Configure git credential helper
RUN git config --global credential.helper store

# Set environment variable for memory optimization
ENV PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

# Expose Gradio port
EXPOSE 7860

# This is the base - no code copied yet
# Code will be added in the next stage
